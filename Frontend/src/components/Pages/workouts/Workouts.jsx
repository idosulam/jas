import './Workouts.css';

function Workouts() {
  return (
    <section className="page">
      <h1 className="page__title animate-in animate-in--1">Workouts</h1>
      <p className="page__subtitle animate-in animate-in--2">
        Browse routines and start a session.
      </p>
    </section>
  );
}

export default Workouts;
